// Import modules

// Gulp modules
import gulp_             from 'gulp';               export const gulp = gulp_
import gulp_batch        from 'gulp-batch';         export const batch = gulp_batch
import gulp_folder       from 'gulp-folders';       export const folder = gulp_folder
import gulp_if           from 'gulp-if';            export const gulpIf = gulp_if
import gulp_notify       from 'gulp-notify';        export const notify = gulp_notify
import gulp_plumber      from 'gulp-plumber';       export const plumber = gulp_plumber
import gulp_rename       from 'gulp-rename';        export const rename = gulp_rename
import gulp_sourcemaps   from 'gulp-sourcemaps';    export const sourcemaps = gulp_sourcemaps
import gulp_watch        from 'gulp-watch';         export const watch = gulp_watch

// Webpack modules
import webpack_          from 'webpack';            export const webpack = webpack_
import webpack_stream    from 'webpack-stream';     export const webpackStream = webpack_stream
import webpack_dev_middleware    from 'webpack-dev-middleware';     export const webpackDevMiddleware = webpack_dev_middleware
import webpack_hot_middleware    from 'webpack-hot-middleware';     export const webpackHotMiddleware = webpack_hot_middleware

// System modules
import browser_sync      from 'browser-sync';       export const browserSync = browser_sync
import es_               from 'event-stream';       export const es = es_
import fs_               from 'fs';                 export const fs = fs_
import path_             from 'path';               export const path = path_

// JavaScript modules
import gulp_babel        from 'gulp-babel';         export const babel = gulp_babel
import gulp_flowtype     from 'gulp-flowtype';      export const flow = gulp_flowtype
import gulp_uglify       from 'gulp-uglify';        export const uglify = gulp_uglify

// Html modules
import gulp_pug          from 'gulp-pug';           export const pug = gulp_pug

// Style modules
import gulp_autoprefixer from 'gulp-autoprefixer';  export const autoprefixer = gulp_autoprefixer
import gulp_clean_css    from 'gulp-clean-css';     export const clean = gulp_clean_css
import gulp_csscomb      from 'gulp-csscomb';       export const csscomb = gulp_csscomb
import gulp_postcss      from 'gulp-postcss';       export const postcss = gulp_postcss
import gulp_stylus       from 'gulp-stylus';        export const stylus = gulp_stylus
import kouto_swiss       from 'kouto-swiss';        export const koutoSwiss = kouto_swiss
import lost_             from 'lost';               export const lost = lost_
import rupture_          from 'rupture';            export const rupture = rupture_

// Image modules
import gulp_spritesmith  from 'gulp.spritesmith';   export const spritesmith = gulp_spritesmith
import gulp_svg_sprite   from 'gulp-svg-sprite';    export const svgSprite = gulp_svg_sprite
